var Word = require("./Word.js");
var inquirer = require("inquirer");

var wordArray = ["PASS JUDGMENT", "CONGRATULATIONS", "QUALITY", "RALLY", "TONED DOWN", "PROVE", "UNSURPASSED"];
var letters = /[a-zA-Z]/;

var numGuesses;

function playGame(){
    
}